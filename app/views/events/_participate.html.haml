= form_with model: event.participations.build, url: event_participations_path(event),remote: true do |f|
  = hidden_field_tag :event_id, event.id
  = f.submit "イベントに参加する", class: "btn btn-primary text-white"